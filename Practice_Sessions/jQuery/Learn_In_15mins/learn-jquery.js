//Tells computer to not fire off the JavaScript until the document is parsed by the browser
//The dollar sign means "jquery" you can also replace it wby typing "jQuery(document).ready(function(){})"
$(document).ready(function(){
    // BASIC SELECTORS - They work like CSS selectors
        //
    // BASIC ANIMATIONS

    //INDEX FILTERS

    // RELATIONSHIP FILTERS

    //ATTRIBUTE FILTERS
});